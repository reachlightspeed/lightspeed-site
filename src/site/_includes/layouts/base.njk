<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>Lightspeed - A Web Performance Optimization Consultancy</title>
    <meta name="description" content="Lightspeed is a performance optimization consultancy that works with forward-thinking companies to create inconceivably fast digital experiences.">
    <meta name="author" content="Lightspeed, LLC">  
    {% set css %}
      {% include "../../../../dist/css/styles.css" %}
    {% endset %}
    {# begin tailwinds #}
    <style>
    {{ css | cssmin | safe }}
    </style>
    {# Begin manual styles #}
    <style>  
    html {
      box-sizing: border-box;
      font-family: -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica Neue,Arial,Noto Sans,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;
      line-height: 1.5;
    }
    *, *:before, *:after { box-sizing: inherit }
    h1, h2, h3, h4, h5, h6 { line-height: 1 }
    img { max-width: 100%; height: auto }

    body {
      background-image: linear-gradient(180deg, #ffffff 0%, #e3f2ff 100%);
      background-attachment: fixed;
    }

    p {
      font-size: 1.25rem;
      font-weight: 300;
    }

    h3 {
      font-size: 2rem;
      font-weight: 500;
      color: #1e2a61;
      letter-spacing: -0.0125rem;
      margin: 0;
    }

    main {
      display: block;
      max-width: 45rem;
      margin: 0 auto;
    }

    .proton {
      display: block;
      margin: 4rem auto 1rem;
    }

    .site-name {
      font-size: 6rem;
      font-style: oblique;
      font-weight: 600;
      color: #3b5ee2;
      letter-spacing: -0.24rem;
      text-align: center;
      margin: 0;
    }
    .site-description {
      max-width: 42rem;
      font-size: 2.75rem;
      font-weight: 200;
      color: #1E2A61;
      letter-spacing: 0;
      text-align: center;
      margin: 1rem auto;
    }
    .box {
      border-top: 6px solid #3b5ee2;
      background: rgba(255,255,255,0.89);
      padding: 2.75rem;
      margin-top: 4rem;
      box-shadow: 0 2px 7px 0 rgba(7,7,22,0.12);
      border-radius: 8px;
      -webkit-backdrop-filter: saturate(140%) blur(16px);
      backdrop-filter: saturate(140%) blur(16px);
    }
    .hidden {
      display: none;
    }
    .box.offerings ::marker {
      font-size: 1.5rem;
      color: #1e2a61;
    }
    .box.offerings ol p {
      font-weight: 400;
      color: #1e2a61;
      font-size: 1.5rem; 
      margin-bottom: 0px;
      line-height: 1.2;
    }
    .box.offerings ol span {
      font-size: .9em;
    }
    .box.who img {
      height: 150px;
      width: 150px;
      border-radius: 100%;
      margin: auto;
      display: block;
    }
    .box.who .bio-container {
      display: flex;
    }
    .box.who .bio {
      margin-top: 20px;
      display: inline-block;
      vertical-align: top;
      width: 50%;
    }
    .box.who .bio p.name {
      margin-bottom: 0px;
    }
    .box.who .bio p.title {
      font-size: .9rem;
      margin-top: 0px;
    } 
    .box.who .bio p.name, .box.who .bio p.title {
      text-align: center;
    }
    .box.contact input, .box.contact textarea {
      width: 100%;
    }
    .contact input, .contact textarea {
      line-height: 2.25rem;
      font-size: 1.25rem;
      padding: .5rem;     
      border: 1px solid #cbcbcc;
    }
    .contact textarea {
      resize: none;
    }
    .contact ::placeholder {
      color: #828181
    }
    .contact .send-it {
      text-align: right;
    }
    .contact .send-it button {
      width: 6rem;
      height: 3rem;
      background-color: #3b5ee2;
      color: #fefeff;
      border: none;
      cursor: pointer;
      font-size: 1.25rem;
      transition: .5s;
    }
    .contact .send-it button:hover {
      background-color: #1e2a61;
    }
    footer {
      margin-top: 50px;
    }
    footer p {
      margin: 0;
      text-align: center;
      font-size: .8rem;
    }

    @media screen and (max-width: 420px) {
      .site-name {
        font-size: 4rem;
      }
      .site-description {
        font-size: 2.25rem;
      }
      .box.who .bio-container {
        display: block;
      }
      .box.who .bio {
        width: 100%;
      }
    } 
    </style>
  </head>
	
	<body>
    {{ content | safe }}
    {% set js %}
      {% include "js/index.js" %}
    {% endset %}
    <script>
      {{ js | jsmin | safe }}
    </script>
  </body>
</html>
